<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to root_path, class: "navbar-brand" do %>
  <div class="d-flex align-items-center justify-content-between">
    <%= image_tag "logo.png" %>
    <p class="title-nav-bar">LITTLE</br><strong>MONSTER</strong></p>
  </div>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">

    <% if user_signed_in? %>
      <li class="nav-item active">
        <% if policy(Animal).create? %>
          <%= link_to "Add my monster", new_animal_path, class: 'btn btn-flat-purple btn-sm' %>
        <% end %>
      </li>

      <li class="nav-item dropdown">
        <%= image_tag "shrek.jpg", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>

        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <span class="email"><%= current_user.email %> </span>
          <% if policy(Animal).create? %>
            <%= link_to "➕ My profile", "#", class: "dropdown-item" %>
          <% end %>
          <%= link_to "🔜 My bookings", dashboard_path, class: "dropdown-item" %>
          <%= link_to "🔚 Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
        </div>
      </li>
      <% else %>
      <li class="nav-item">
        <%= link_to "Login", new_user_session_path, class: "nav-link" %>
      </li>
    <% end %>
    </ul>
  </div>
</div>
